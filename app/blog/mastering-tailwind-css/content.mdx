import CodeWindow from '@/components/ui/code-window';

# Mastering Tailwind CSS for Rapid UI Development

Tailwind CSS has revolutionized the way we approach web styling, offering a utility-first framework that enables rapid UI development without sacrificing flexibility.

## Why Tailwind CSS?

Tailwind CSS provides several advantages:

- Rapid prototyping
- Consistent design system
- Highly customizable
- Small production bundles
- Great developer experience

## Getting Started

Install Tailwind CSS in your project:

<CodeWindow
  code={`npm install tailwindcss postcss autoprefixer
npx tailwindcss init -p`}
  language="bash"
  title="Terminal"
/>

Configure your template paths:

<CodeWindow
  code={`// tailwind.config.js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./app/**/*.{js,ts,jsx,tsx}",
    "./components/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}`}
  language="javascript"
  title="tailwind.config.js"
/>

## Key Concepts

### Utility Classes

<CodeWindow
  code={`<div className="flex items-center justify-between p-4 bg-white shadow-md rounded-lg">
  <h1 className="text-xl font-bold text-gray-800">
    Hello World
  </h1>
  <button className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors">
    Click me
  </button>
</div>`}
  language="jsx"
  title="Utility Classes Example"
/>

### Responsive Design

<CodeWindow
  code={`<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  <div className="p-4 bg-white rounded-lg shadow">
    <h2 className="text-lg font-semibold">Card 1</h2>
  </div>
  <div className="p-4 bg-white rounded-lg shadow">
    <h2 className="text-lg font-semibold">Card 2</h2>
  </div>
  <div className="p-4 bg-white rounded-lg shadow">
    <h2 className="text-lg font-semibold">Card 3</h2>
  </div>
</div>`}
  language="jsx"
  title="Responsive Design Example"
/>

### Dark Mode

<CodeWindow
  code={`<div className="bg-white dark:bg-gray-800 text-gray-900 dark:text-white">
  <h1 className="text-2xl font-bold">
    Automatic Dark Mode
  </h1>
  <p className="text-gray-600 dark:text-gray-300">
    This content adapts to the user's preference
  </p>
</div>`}
  language="jsx"
  title="Dark Mode Example"
/>

## Advanced Techniques

### Custom Components

<CodeWindow
  code={`// Button.tsx
interface ButtonProps {
  variant?: 'primary' | 'secondary';
  size?: 'sm' | 'md' | 'lg';
  children: React.ReactNode;
}

export function Button({ 
  variant = 'primary',
  size = 'md',
  children 
}: ButtonProps) {
  const baseClasses = 'rounded-lg font-medium transition-colors';
  const variants = {
    primary: 'bg-blue-600 text-white hover:bg-blue-700',
    secondary: 'bg-gray-200 text-gray-800 hover:bg-gray-300'
  };
  const sizes = {
    sm: 'px-3 py-1.5 text-sm',
    md: 'px-4 py-2 text-base',
    lg: 'px-6 py-3 text-lg'
  };

  return (
    <button className={\`\${baseClasses} \${variants[variant]} \${sizes[size]}\`}>
      {children}
    </button>
  );
}`}
  language="typescript"
  title="Component Example"
/>

### Custom Utilities

<CodeWindow
  code={`// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      spacing: {
        '128': '32rem',
        '144': '36rem',
      },
      colors: {
        brand: {
          light: '#3AACFF',
          DEFAULT: '#0096FF',
          dark: '#0077CC',
        },
      },
      animation: {
        'fade-in': 'fadeIn 0.5s ease-out',
        'slide-up': 'slideUp 0.5s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideUp: {
          '0%': { transform: 'translateY(20px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
      },
    },
  },
}`}
  language="javascript"
  title="Custom Configuration"
/>

## Best Practices

1. Use meaningful class order:
   - Layout (position, display, width, height)
   - Spacing (margin, padding)
   - Typography (font, text)
   - Visual (colors, shadows, borders)
   - Interactive (hover, focus)

2. Extract components for reusability:

<CodeWindow
  code={`function Card({ title, children }) {
  return (
    <div className="p-6 bg-white dark:bg-gray-800 rounded-lg shadow-lg">
      <h2 className="text-xl font-semibold mb-4">{title}</h2>
      {children}
    </div>
  );
}

function Badge({ variant = 'primary', children }) {
  const variants = {
    primary: 'bg-blue-100 text-blue-800',
    success: 'bg-green-100 text-green-800',
    warning: 'bg-yellow-100 text-yellow-800',
  };

  return (
    <span className={\`px-2 py-1 rounded-full text-sm \${variants[variant]}\`}>
      {children}
    </span>
  );
}`}
  language="typescript"
  title="Component Extraction"
/>

## Conclusion

Tailwind CSS provides a powerful and flexible approach to styling that can significantly speed up your development process while maintaining high-quality, consistent designs. By understanding its core concepts and following best practices, you can create beautiful, responsive interfaces efficiently.